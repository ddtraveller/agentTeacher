fastapi==0.115.0
uvicorn[standard]==0.30.6
httpx==0.27.2
python-multipart==0.0.9
pydantic==2.9.2

# LlamaIndex for wiki RAG. Embeddings via Ollama (nomic-embed-text on the
# host) — no other inference deps needed since we call Ollama directly for
# chat generation.
llama-index-core==0.11.23
llama-index-embeddings-ollama==0.3.1
llama-index-readers-file==0.2.2
